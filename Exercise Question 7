"""Given two sets, Checks if One Set is Subset or superset of Another Set. if the subset is found 
delete all elements from that set. """

beta = {57, 83, 29}
alpha = {67, 73, 43, 48, 83, 57, 29}
result = {}

            #### Answer1 ####
# if len(alpha) > len(beta):
#     print('alpha is superset and beta is subset')
#     for x in beta:
#         alpha.remove(x)
#         result = alpha
## elif len(alpha) < len(beta):
#     print('beta is superset and alpha is subset')
#     for x in alpha:
#         beta.remove(x)
#         result = beta
## else:
#     print('two sets are equal')
#     for x in alpha:
#         beta.remove(x)
#         result = beta
## print('the new super set  = ' , '\t',result)

            ### Answer2 ####

if alpha.issubset(beta):
    print('beta is superset and alpha is subset')
    for x in alpha:
        beta.remove(x)
        result = beta

elif beta.issubset(alpha):
    print('alpha is superset and beta is subset')
    for x in beta:
        alpha.remove(x)
        result = alpha
else:
    if len(alpha) > len(beta):
        print('alpha is superset and beta is subset')
    elif len(alpha) < len(beta):
        print('beta is superset and alpha is subset')
    else:
        print('BOTH SET EQUAL EACH OTHER BUT NO SUBSET  FOUND')

print('the result list  = ' ,'\t',result)
